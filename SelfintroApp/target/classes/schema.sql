CREATE TABLE IF NOT EXISTS selfintroapp (
	id VARCHAR(50) PRIMARY KEY
	, month VARCHAR(50)
	, age INT
	, profession VARCHAR(50)
	, description VARCHAR(50)
	, remarks VARCHAR(50)
)DEFAULT CHARSET=utf8mb4;